## interactiveai images build

Build a container image with Docker

### Synopsis

Build a container image using the local Docker CLI.

This is a thin wrapper around 'docker build' that requires an explicit tag,
Dockerfile, and build context.

```
interactiveai images build [image_name] [flags]
```

### Options

```
  -c, --context string   Build context directory (default: current directory) (default ".")
  -f, --file string      Path to the Dockerfile (default: ./Dockerfile) (default "Dockerfile")
  -h, --help             help for build
  -t, --tag string       Tag suffix to append to the fixed registry (e.g. 1.2.3)
```

### Options inherited from parent commands

```
      --deployment-hostname string   Hostname for the deployment API (default "https://deployment.dev.interactive.ai")
      --hostname string              Hostname for the API (default "https://dev.interactive.ai")
```

### SEE ALSO

* [interactiveai images](interactiveai_images.md)	 - Build and manage container images

###### Auto generated by spf13/cobra on 4-Dec-2025
