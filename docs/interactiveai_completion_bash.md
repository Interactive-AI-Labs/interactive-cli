## interactiveai completion bash

Generate the autocompletion script for bash

### Synopsis

Generate the autocompletion script for the bash shell.

This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

To load completions in your current shell session:

	source <(interactiveai completion bash)

To load completions for every new session, execute once:

#### Linux:

	interactiveai completion bash > /etc/bash_completion.d/interactiveai

#### macOS:

	interactiveai completion bash > $(brew --prefix)/etc/bash_completion.d/interactiveai

You will need to start a new shell for this setup to take effect.


```
interactiveai completion bash
```

### Options

```
  -h, --help              help for bash
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --deployment-hostname string   Hostname for the deployment API (default "https://deployment.dev.interactive.ai")
      --hostname string              Hostname for the API (default "https://dev.interactive.ai")
```

### SEE ALSO

* [interactiveai completion](interactiveai_completion.md)	 - Generate the autocompletion script for the specified shell

###### Auto generated by spf13/cobra on 4-Dec-2025
